<UserControl x:Class="win.WPF.aDrumsManager.Views.TriggerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:win.WPF.aDrumsManager.Views"
             xmlns:mvvm="http://prismlibrary.com/"
             mc:Ignorable="d" 
             mvvm:ViewModelLocator.AutoWireViewModel="True"
             d:DesignHeight="300" d:DesignWidth="300">
    <DockPanel>
        <StackPanel Orientation="Vertical" DockPanel.Dock="Top">
            <DockPanel>
                <Label Content="Trigger Type:" Width="150" HorizontalContentAlignment="Right" DockPanel.Dock="Left" />
                <ComboBox ItemsSource="{Binding Path=TriggerTypes, Mode=OneWay}" SelectedItem="{Binding Path=TriggerType, Mode=TwoWay}" />
            </DockPanel>
            <DockPanel>
                <Label Content="Trigger Curve:" Width="150" HorizontalContentAlignment="Right" DockPanel.Dock="Left" />
                <ComboBox ItemsSource="{Binding Path=CurveTypes, Mode=OneWay}" SelectedItem="{Binding Path=Trigger.Curve, Mode=TwoWay}" />
            </DockPanel>
        </StackPanel>
    </DockPanel>
</UserControl>
